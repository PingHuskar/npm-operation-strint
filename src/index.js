const snumpat = require(`./snumpat`);

const numinbase10 = require(`./numinbase10`);

const sum = require(`./sum`);
const minus = require(`./minus`);
const multiplybysum = require(`./multiplybysum`);
const fact = require('./fact')

const CSRange = require(`./CSRange`);

const IsMoreThan = require(`./IsMoreThan`);

const divby2 = require(`./divby2`);
const multiply = require(`./multiply`);

const pow = require(`./pow`);
const photo = require(`./photo`);

module.exports = {
  numinbase10,
  snumpat,
  sum,
  minus,
  multiplybysum,
  multiply,
  fact,
  CSRange,
  IsMoreThan,
  divby2,
  pow,
  photo,
};
